{
  "name": "tiny-router",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rimraf ./docs && npm run clean --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "./packages/pattern-parser",
    "./packages/pattern-compiler",
    "./packages/router"
  ],
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.0.0",
    "@types/jest": "^27.0.1",
    "jest": "^27.0.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.50.5",
    "terser": "^5.7.0",
    "ts-jest": "^27.0.2",
    "tslib": "^2.3.1",
    "typescript": "^4.4.3"
  }
}
